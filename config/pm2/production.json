{
  "apps": [{
    "name": "HotVenue",
    "script": "./bin/www",
    "instances": 0,
    "exec_mode": "cluster",
    "max_restarts": 5,
    "max_memory_restart": "450M",
    "env": {
      "NODE_ENV": "production"
    }
  }]
}
